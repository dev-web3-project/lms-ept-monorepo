# ============================================================
# CONFIG — university-service (Cycles, Départements, Classes)
# ============================================================

server:
  port: 8082

spring:
  datasource:
    url: ${UNIVERSITY_DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
