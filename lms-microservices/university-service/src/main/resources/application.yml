# university-service — Configuration minimale locale
# Config centralisée dans config-repo/university-service.yml

spring:
  application:
    name: university-service
  config:
    import: "optional:configserver:http://localhost:8888"

# --- Fallback local ---
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
